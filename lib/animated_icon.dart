import 'package:flutter/material.dart';

class FanimatedIcon extends StatelessWidget {
  const FanimatedIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}